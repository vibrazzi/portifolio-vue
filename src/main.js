// Importa o Vue.js
import { createApp } from 'vue';

// Importa os estilos globais (incluindo Tailwind CSS)
import './style.css';

// Importa o componente raiz da aplicação
import App from './App.vue';

// Cria e monta a aplicação Vue no elemento com o ID "app"
createApp(App).mount('#app');
